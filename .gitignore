# API keys and credentials — never commit
api/.env
api/*.rtf
*.rtf

# Local database — personal betting data
data/bets.db

# Virtual environment — too large, regenerate with pip install
.venv/

# Cached historical data — regenerated by build_historical_dataset.py
cache/

# Daily cache files
debug_context_*.json

# Log output
logs/

# Python
__pycache__/
*.pyc
*.pyo
.DS_Store

# Jupyter
.ipynb_checkpoints/

# Hookify rules — local/personal, not shared
.claude/*.local.md
.worktrees/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
